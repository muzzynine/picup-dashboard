{
  "name": "picup-admin",
  "version": "0.0.1",
  "description": "picup-admin-dashboard",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "react-build": "browserify -t [ babelify --presets [ react ] ] public/js/index.js -o public/bundle.js"
  },
  "author": "muzzynine",
  "license": "MIT",
  "dependencies": {
    "babelify": "^7.2.0",
    "body-parser": "^1.15.0",
    "express": "^4.13.4",
    "flux": "^2.1.1",
    "morgan": "^1.7.0",
    "react": "^0.14.7",
    "react-dom": "^0.14.7",
    "react-router": "^2.0.1"
  }
}
